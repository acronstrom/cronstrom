# Umami Cloud (https://cloud.umami.is): Website ID from your site settings.
# Must be present when running `vite build` (e.g. set the same name in Vercel → Environment Variables).
VITE_UMAMI_WEBSITE_ID=
